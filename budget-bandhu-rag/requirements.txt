# ── Core API ──────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
python-multipart>=0.0.9

# ── MongoDB Atlas (Motor async driver) ───────────────────────────────
motor>=3.3.0
pymongo>=4.6.0

# ── SQLite async (Tier 1 working memory only) ────────────────────────
aiosqlite>=0.20.0

# ── Embeddings (all-MiniLM-L6-v2, 384-dim) ──────────────────────────
sentence-transformers>=2.7.0
numpy>=1.26.0

# ── LangGraph / LangChain ─────────────────────────────────────────────
langgraph>=0.1.0
langchain-core>=0.2.0

# ── Scheduling (memory decay jobs) ───────────────────────────────────
APScheduler>=3.10.0

# ── Web3 / Blockchain (backend minting) ──────────────────────────────
web3>=6.20.0              # backend wallet minting (GoalBadgeSBT via HTTP provider)

# ── Image Generation (badge + certificate NFT art) ────────────────────
Pillow>=10.3.0            # PIL — generates badge (400×400) / certificate (800×560) PNGs

# ── Other Utils ──────────────────────────────────────────────────────
certifi
pyngrok
passlib
python-jose[cryptography]
qdrant-client
neo4j
pandas
